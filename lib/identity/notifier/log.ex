defmodule Identity.Notifier.Log do
  @moduledoc """
  Default notifier that simply logs actions using Elixir's `Logger`.

  For more information, see `Identity.Notifier`.
  """
  use Identity.Notifier
end
